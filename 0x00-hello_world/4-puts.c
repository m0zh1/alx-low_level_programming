#include <stdio.h>

/*
 * main - Entry point
 * Description: prints the phrase "\"Programming is like building a 
 * multilingual puzzle"
 * Return: 0 if exited gracefully, otherwise non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
