#include <stdio.h>

/*
 * Output: "Programming is like building a multilingual puzzle" using puts
 * return 0 if exited without err, otherwise return non-zero
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return 0;
}
