#include "main.h"

/**
 * add - adds two ints and returns result
 * @a: type in, first argument
 * @b: tpe int, second argument
 * Return: a + b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
